/**
 * Created by ruiz_p on 30/10/13.
 */

define([], function() {

	function GTween() {
		this._startDate = null;
		this._endDate = null;

		this._startX = 0;
		this._startY = 0;
		this._startScale = 0;
		this._startRotation = 0;
		this._startAlpha = 0;

		this._stopX = 0;
		this._stopY = 0;
		this._stopScale = 0;
		this._stopRotation = 0;
		this._stoptAlpha = 0;
	}

	GTween.prototype.update = function(gsprite, delta) {
		// Traiter le gsprite avec le delta

		// Traiter position x

		// Traiter position y

		// Traiter scale

		// Traiter rotation

		// Traiter Alpha
	};

	return GTween;
});